//! ` `
// @<Append the new box to the current vertical list...@>=
macro_rules! Append_the_new_box_to_the_current_vertical_list__followed_by_the_list_of_special_nodes_taken_out_of_the_box_by_the_packager {
    ($globals:expr) => {{
        // append_to_vlist(just_box);
        // if adjust_head<>adjust_tail then
        //   begin link(tail):=link(adjust_head); tail:=adjust_tail;
        //    end;
        // adjust_tail:=null
        todo!();
    }}
}

//! ` `

// @<Output the whatsit node |p| in a vlist@>=
pub(crate) macro Output_the_whatsit_node_p_in_a_vlist($globals:expr, $p:expr) {{
    // out_what(p)
    out_what($globals, $p)?;

    use crate::section_1373::out_what;
}}

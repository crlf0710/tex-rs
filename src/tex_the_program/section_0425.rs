//! ` `

// @<Fetch a font dimension@>=
pub(crate) macro Fetch_a_font_dimension($globals:expr) {{
    todo!("Fetch a font dimension")
    // begin find_font_dimen(false); font_info[fmem_ptr].sc:=0;
    // scanned_result(font_info[cur_val].sc)(dimen_val);
    // end
}}
